apiVersion: v1
kind: Service
metadata:
  name: {{ include "memory-store.fullname" . }}
  labels:
    {{- include "memory-store.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800  # 3 hours - keeps same agent on same pod for memory consistency
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "memory-store.selectorLabels" . | nindent 4 }}
