apiVersion: v1
kind: Service
metadata:
  name: {{ include "dbx-sql-mcp.fullname" . }}
  labels:
    {{- include "dbx-sql-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dbx-sql-mcp.selectorLabels" . | nindent 4 }}
